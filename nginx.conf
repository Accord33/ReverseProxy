events {
    worker_connections  16;
}
http {
    server {
        listen 443;
        location / {
            proxy_pass https://ict-lab.toyo-ushiku.jp/;
            proxy_redirect default;
        }
    }
}
